<%= form_for([current_user, @collection, @image = current_user.images.build].compact) do |f| %>
  <%= render 'common/errors', resource: @image %>
  <%= f.text_field :source_url, placeholder: 'http://', required: true, autofocus: true, autocomplete: :off %>
<% end %>
