<%= form_for([current_user, @collection = current_user.collections.build]) do |f| %>
  <%= render 'common/errors', resource: @collection %>
  <%= f.text_field :title, placeholder: 'Add new collection', required: true, autofocus: true, autocomplete: :off %>
<% end %>
